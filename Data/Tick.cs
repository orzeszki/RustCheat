using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace GorgaTech.Data
{
    class Tick
    {
        public static void UpdateTick(Packet p)
        {
           /* var id = p.EntityID();
            PlayerTick cos = global::PlayerTick.Deserialize(p);
            try{
                 Form2.myModelState = "id: "+id+" | "+cos.modelState.flags;
            }catch (Exception ex){
                Form2.myModelState = ex.Message;
            }*/
        }
    }
}
